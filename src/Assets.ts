module Coiniac {

    export class Assets {
        static readonly TICKER_BTC = {
            key: 'btc',
            path: 'assets/tickers/btc.png'
        };

        static readonly TICKER_ETH = {
            key: 'eth',
            path: 'assets/tickers/eth.png'
        };

        static readonly TICKER_XRP = {
            key: 'xrp',
            path: 'assets/tickers/xrp.png'
        };

        static readonly IMG_PRELOADER_BAR = {
            key: 'preloaderBar',
            path: 'assets/loader.png'
        };
    }
}
